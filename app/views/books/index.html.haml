- @books.each_slice(3) do |books|
  .row
    - books.each do |book|
      .col-md-4
        .book
          .image_wrapper
            = link_to book
          #book_title
            %h2= link_to book.title, book
